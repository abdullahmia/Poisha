name: Create Development Builds

on:
  push:
    branches: ["main"]

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: development
  build_ios:
    type: build
    params:
      platform: ios
      profile: development
