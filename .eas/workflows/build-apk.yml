name: Build APK

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  build_apk:
    name: Build Android APK
    type: build
    params:
      platform: android
      profile: local
