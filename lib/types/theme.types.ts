export type TColorScheme = 'light' | 'dark';

export interface TPalette {
  bg: string;
  surface: string;
  surfaceAlt: string;
  ink: string;
  inkSoft: string;
  inkMuted: string;
  accent: string;
  accentSoft: string;
  line: string;
  shadow: string;
  danger: string;
  dangerSoft: string;
}
